{
  "name": "zero-oracle-contracts",
  "version": "1.0.0",
  "description": "ZeroOracle smart contracts for 0G Chain",
  "main": "hardhat.config.js",
  "scripts": {
    "compile": "npx hardhat compile",
    "deploy": "npx hardhat run scripts/deploy.js --network 0g-testnet",
    "deploy:mainnet": "npx hardhat run scripts/deploy.js --network 0g-mainnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0"
  }
}
